// Lazy wrapper — self-contained page (no required props)
import { NotificationPreferencesPanel } from '../components/settings/NotificationPreferences';
export default function NotificationPrefsPage() {
  return <NotificationPreferencesPanel />;
}
